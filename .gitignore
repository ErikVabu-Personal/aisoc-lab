# Terraform
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl
crash.log
crash.*.log

# Local tfvars (contains secrets)
terraform.tfvars
*.auto.tfvars

# Local config (contains secrets — admin password, user roster, etc).
# Copy aisoc.config.example -> aisoc.config and edit; aisoc_demo.sh
# sources it automatically if present.
/aisoc.config

# OS
.DS_Store
Thumbs.db

# Python bytecode
__pycache__/
*.pyc


# ship control panel
ship-control-panel/node_modules
ship-control-panel/.next
ship-control-panel/.env*
