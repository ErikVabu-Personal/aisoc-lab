# Phase 3 — Deploy AISOC Runner (DEPRECATED)

Runner deployment has been consolidated into:

- `terraform/2-deploy-aisoc` (runner.tf)

This folder is kept temporarily to avoid breaking old bookmarks.
Do not apply from here.
