name: Deploy SOC Gateway Function

on:
  workflow_dispatch:
    inputs:
      function_app_name:
        description: "Azure Function App name (e.g. func-foundry-soc-xxxxx)"
        required: true
      resource_group:
        description: "Resource group name (e.g. rg-sentinel-test)"
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Azure login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Build function package (with dependencies)
        shell: bash
        run: |
          set -euo pipefail
          cd terraform/2-deploy-aisoc/foundry/function_app

          python -m pip install --upgrade pip
          rm -rf .python_packages function_app.zip

          # Install dependencies into the Azure Functions expected folder
          python -m pip install --no-cache-dir --target ".python_packages/lib/site-packages" -r requirements.txt

          # Sanity check: fail fast if requests didn't land in the package
          python - <<'PY'
          import sys, pathlib
          sys.path.insert(0, str(pathlib.Path('.python_packages/lib/site-packages').resolve()))
          import requests  # noqa: F401
          print('requests_ok')
          PY

          # Zip must have host.json at root
          zip -r function_app.zip host.json requirements.txt SOCGateway shared .python_packages

      - name: Deploy zip to Function App
        shell: bash
        run: |
          set -euo pipefail
          cd terraform/2-deploy-aisoc/foundry/function_app
          az functionapp deployment source config-zip \
            --resource-group "${{ inputs.resource_group }}" \
            --name "${{ inputs.function_app_name }}" \
            --src function_app.zip
